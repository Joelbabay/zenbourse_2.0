{% extends 'base.html.twig' %}

{% block title %}{{ valueTitle }} - Bibliothèque{% endblock %}

{% block body %}
{% include 'partials/header.html.twig' %}
    <div class="custom-container mt-4">
        <div class="row">
            <div class="col-md-9">
                <h1>{{ valueTitle }}</h1>
                {% if introduction %}
                    <div class="container bg-light mb-4 p-3">
                        <p class="text-justify">{{ introduction|raw }}</p>
                    </div>
                {% endif %}
            </div>
            
            <div class="col-md-3">
                {% include 'investisseur/widgets/sidebar-stocks.html.twig' with {
                    'stocks': examples,
                    'valueTitle': valueTitle,
                    'category': category
                } %}
            </div>
        </div>
    </div>
{% endblock %} 